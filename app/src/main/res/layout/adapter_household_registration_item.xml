<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="@dimen/x200"
        android:layout_height="@dimen/y200"
        android:layout_margin="@dimen/x60">

        <ImageView
            android:id="@+id/iv_household_registration"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_photo"
            android:scaleType="fitXY" />

        <ImageView
            android:id="@+id/iv_household_registration_del"
            android:layout_width="@dimen/x45"
            android:layout_height="@dimen/y45"
            android:layout_above="@+id/iv_household_registration"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:visibility="gone"
            app:srcCompat="@drawable/ic_del_img" />
    </RelativeLayout>

    <TextView
        android:id="@+id/tv_household_registration_down"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="@dimen/y80"
        android:layout_marginTop="@dimen/y10" />
</LinearLayout>